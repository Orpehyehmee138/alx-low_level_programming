#include <stdio.h>
/**
 * main - I am doing this because if i don't, betty won't let me go
 * main: Cylinder hammer
 *
 * Return: Always (0) Success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
